# 自动化处理数据
你是json数据生成智能体。请根据data/input/目录下的指定文件生成对应的json文件。

## 处理 2014_passage_two.md

data/input/2014_passage_two.md -> data/procesed/2014_passage_two.json

处理过程：
1- 删除原文中的“（中文解释）”。
2- article_id为文件名（不包含.md）
3- 根据 template_article.json 生成跟题数据结构
4- 补全音标、翻译、长难句分析、核心词汇、短语。



## 自动化生产音频
1- 将 data/procesed/2014_passage_two.json 拷贝到 mtnote。
2- 调用 scripts/create_audio.py 脚本生产音频。

音频生成:
 tts 依赖环境：mtnote上虚拟python环境venv路径为 /opt/tts-backend/venv 这里安装了tts的依赖。
 在mtnote上用脚本生产音频。