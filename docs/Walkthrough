# 手机端深度重构：极简沉浸式阅读空间

根据反馈，我们对手机端进行了“独立”重构。目前的策略是：**PC 端保留原有分栏布局，手机端开启全屏沉浸模式**。

## 手机端重大更新

### 1. 极致化的屏幕占比 (Reading-First)
- **取消 Header 占用**：手机端不再显示顶部的导航标题栏，垂直阅读空间增加了约 15%。
- **“无框”沉浸感**：移除了手机端所有卡片边框、阴影和背景块。内容直接铺满深色背景，如同沉浸式电子书。
- **智能排版**：
    - **竖屏**：自动切换为单列，原文全屏宽度，译文智能叠放，最大化文字清晰度。
    - **横屏**：极致压缩边距，让文字横跨屏幕。

### 2. 隐藏式交互 (Interactive UI)
- **悬浮设置按钮 (FAB)**：右下角新增了一个半透明的 ⚙️ 设置图标。点击后弹出全屏菜单，可在此切换“阅读/学习模式”或“选择文章”。
- **透明音频中心**：底部的音频控制台改为半透明毛玻璃浮窗，不干扰视觉重心的同时保证功能。

### 3. PC 与手机的完美分立
- 我们通过响应式逻辑确保了 PC 端的原有美感不受任何影响。你可以在同一套代码中，于大屏幕上享受分栏效率，在小屏幕上享受纯净阅读。

---
> [!IMPORTANT]
> **操作提示**：
> - 手机端点击右下角 **⚙️** 即可唤起菜单更换文章或模式。
> - 菜单弹出时支持模糊背景（Glassmorphism），保障视觉舒适度。
